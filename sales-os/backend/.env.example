# FastAPI 服务配置
APP_ENV=development
HOST=127.0.0.1
PORT=8000

# SQLite 数据库文件；生产环境可替换为 PostgreSQL 连接层
DATABASE_PATH=./data/sales_os.db

# 允许访问后端的前端源，多个地址用英文逗号分隔
FRONTEND_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
